import { TimeDurationDirective } from './time-duration.directive';

describe('TimeDurationDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeDurationDirective();
    expect(directive).toBeTruthy();
  });
});
